namespace ex1._5
{
    using System;
    using System.Collections.Generic;
    using System.Linq;

    class Program
    {
        static void Main()
        {
            /*
            List<int> numbers = Console.ReadLine()
                .Split(", ", StringSplitOptions.RemoveEmptyEntries)
                .Select(int.Parse)
                .ToList();

            List<int> powerof2 = new List<int>();
            List<int> powertwo = new List<int>();

            for (int a = 0; a < numbers.Count; a++)
                powertwo.Add(numbers[a]);

            Console.WriteLine(string.Join(" ", powertwo));

            for (int i = 0; i < powertwo.Count; i++)
            {   /*
                while (true)
                {
                    powertwo[i] = powertwo[i] / 2;
                    if (powertwo[i] == 2)
                    {
                        powerof2.Add(numbers[i]);
                        Console.WriteLine(string.Join(" ", powerof2));
                        return;
                    }
                }
               
                powertwo[i] = powertwo[i] / 2;
                if (powertwo[i] == 2)
                {
                    powerof2.Add(numbers[i]);
                    Console.WriteLine(string.Join(" ", powerof2));
                }

                Console.WriteLine(string.Join(" ", powerof2));
            }
           */

        }
    }
}
