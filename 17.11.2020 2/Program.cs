namespace _17._11._2020_2
{
    using System;

    class Program
    {
        static void Main()
        {
            int[] array = { 10, 5, 5, 99, 3, 4, 2, 5, 1, 1, 4 };
            // Console.WriteLine(string.Join(", ", array));  печати целия масив, върши същата работа
            // като инт.

            // Array.Sort(array);
            // Array.Reverse(array);
            // Console.WriteLine(string.Join(", ", array));

        }
    }
}
